### Imports

The code imports various modules and components, which are primarily used for UI rendering and state management within a React and Sitecore JSS context:

- **React and Sitecore JSS**: 
  - `FC` from `react` for typing the component as a functional component.
  - `Text` from `@sitecore-jss/sitecore-jss-nextjs` for rendering text fields from Sitecore.

- **Utility and Store Hooks**:
  - `Tokens` from `code/tokens` for accessing predefined token constants.
  - `useStore` from `frontend/hooks/useStore` for accessing the global store state.
  - `useContactUsStore` from `frontend/components/renderings/ContactUs/store/createStore` specifically for state management related to contact form operations.

- **Models and Interfaces**:
  - `IHolidaysStores` from `frontend/store/holidays` which likely contains type definitions for stores related to holiday information.
  - `Tokenizer` from `frontend/utils/tokenizer` for replacing tokens in strings.
  - `SitecoreDictionary` from `models/enum/SitecoreDictionary` for accessing dictionary items.
  - `ISitecoreField` from `models/sitecore/generic/ISitecoreField` which defines the structure for Sitecore fields.

- **UI Components**:
  - `Button` and `Popup` from `frontend/components/common` for rendering buttons and popup modal elements.
  - `RichTextWithLinks` from `frontend/components/common/RichTextWithLinks` for rendering rich text fields that include hyperlinks.

- **Styles**:
  - `styles` from `frontend/components/renderings/ContactUs/ContactForm.module.scss` for specific CSS module styles related to the component.

### Structure

The component `SubmitMessagePopUp` is defined as a functional component using React's Functional Component (FC) type, accepting `ISumbitMessagePopupProps` as props which include:

- `closeSubmitMessage`: A function to close the popup.
- `isSuccess`: A boolean indicating if the popup should show a success or failure message.
- `submitPopupContent`: An object containing various Sitecore fields for dynamic text based on success or failure status.

The component uses hooks to retrieve necessary data:
- `getPhrase` from a custom hook `useStore` for fetching localized phrases.
- `caseNumber` from `useContactUsStore` indicating a unique identifier for the contact submission, if available.

### Logic

1. **Conditional Content**:
   - Depending on whether a `caseNumber` is present, different titles and descriptions are chosen. This is managed by ternary operators that check the presence of `caseNumber`.
   - The button text also changes based on the presence of `caseNumber` and whether the operation was successful or not.

2. **Token Replacement**:
   - If a `caseNumber` is present, the description text includes this number, and it is formatted using the `Tokenizer.replaceTokens` function which injects the `caseNumber` into the text where specified by tokens.

3. **Rendering**:
   - A `Popup` component is used to render the overall modal, with dynamic title and description content based on the operation's success and the presence of a `caseNumber`.
   - The `footerContent` of the `Popup` is generated by a function `renderCloseButton`, which returns a `Button` component with an onClick event bound to `closeSubmitMessage`.
   - The `RichTextWithLinks` component is used to render the description, allowing embedded HTML (like strong tags around the case number) and hyperlinks within the text.

This component effectively handles both success and error states, providing user feedback through a modal popup interface in a Sitecore JSS and React application.