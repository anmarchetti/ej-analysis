---
ID: "e71eb68f-2510-49e3-a62c-3e5b72ddaa85"
Parent: "02f89113-4c03-40f8-988b-5ae62329b681"
Template: "dd22f1b3-bd87-4db2-9e7d-f7a496888d43"
Path: "/sitecore/system/Modules/PowerShell/Script Library/Content Sync/ANC-399 Translations fix"
DB: master
SharedFields:
- ID: "b1a94ff0-6897-47c0-9c51-aa6acb80b1f0"
  Hint: Script
  Value: |
    $translations = @{
        "fr-CH" = @{
           "Phrase" = "Voir le détail"
        }
        "fr-FR" = @{
           "Phrase" = "Voir le détail"
        }
        "de-CH" = @{
          "Phrase" = "Aufschlüsselung anzeigen" 
        }
        "de-DE" = @{
          "Phrase" = "Aufschlüsselung anzeigen"
        }
    }
    
    function create-lang-version($item, $lang, $field, $value) {
        $targetLangItem = Get-Item -Path $item.Paths.FullPath -Language $lang -ErrorAction SilentlyContinue;
        
        if ($targetLangItem -eq $null -or $targetLangItem.Versions.GetVersions().Count -eq 0) {
            Write-Host "Adding language version for item: $($item.Paths.FullPath) in language: $lang"
            Add-ItemLanguage -Item $item -TargetLanguage $lang -IfExist Skip
            $targetLangItem = Get-Item -Path $item.Paths.FullPath -Language $lang -ErrorAction SilentlyContinue;
        }
        
        if ($null -ne $field -and $null -ne $targetLangItem) {
            Write-Host "Editing item: $($item.Paths.FullPath) in language: $lang"
            $targetLangItem.Editing.BeginEdit()
            $targetLangItem[$field] = $value
            
            $targetLangItem.Editing.EndEdit() | Out-Null
        }
    }
    
    function Price-Breakdown-Update(){
        $dataSourcePath = "/sitecore/content/EasyJet/Holidays/Dictionary/Globals/Buttons/View Breakdown"
        $dataSourceItem = Get-Item -Path $dataSourcePath -ErrorAction SilentlyContinue;
        
        # Add translations on Holidays
        if($dataSourceItem -ne $null){
            foreach ($lang in $translations.Keys) {
                create-lang-version -item $dataSourceItem -lang $lang -field "Phrase" -value $translations[$lang]["Phrase"]
                
                Write-Host "Translations added"
            }
        }
        
        Write-Host "Price Breakdown updated on Holidays"
    }
    
    Price-Breakdown-Update
Languages:
- Language: en
  Versions:
  - Version: 1
    Fields:
    - ID: "25bed78c-4957-4165-998a-ca1b52f67497"
      Hint: __Created
      Value: 20240905T123015Z
