---
ID: "7843fbf9-82fa-434b-90cf-fbd366d9527d"
Parent: "02f89113-4c03-40f8-988b-5ae62329b681"
Template: "dd22f1b3-bd87-4db2-9e7d-f7a496888d43"
Path: "/sitecore/system/Modules/PowerShell/Script Library/Content Sync/ANC-273 Spelling mistake fix"
DB: master
SharedFields:
- ID: "b1a94ff0-6897-47c0-9c51-aa6acb80b1f0"
  Hint: Script
  Value: |
    $translations = @{
        "fr-CH" = @{
            "PrivateFeesDescription" = "Notre partenaire de transfert peut facturer des frais supplémentaires pour le transport de matériel sportif. Vous devrez contacter le centre de support client après la réservation."
        }
        "fr-FR" = @{
            "PrivateFeesDescription" = "Notre partenaire de transfert peut facturer des frais supplémentaires pour le transport de matériel sportif. Vous devrez contacter le centre de support client après la réservation."
        }
        "de-CH" = @{
            "PrivateFeesDescription" = "Unser Transferpartner kann zusätzliche Gebühren für den Transport von Sportgeräten erheben. Sie müssen nach der Buchung das Kundendienstzentrum kontaktieren."
        }
        "de-DE" = @{
            "PrivateFeesDescription" = "Unser Transferpartner kann zusätzliche Gebühren für den Transport von Sportgeräten erheben. Sie müssen nach der Buchung das Kundendienstzentrum kontaktieren."
        }
    }
    
    function create-lang-version($item, $lang, $field, $value) {
        $targetLangItem = Get-Item -Path $item.Paths.FullPath -Language $lang -ErrorAction SilentlyContinue;
            
        if ($targetLangItem -eq $null -or $targetLangItem.Versions.GetVersions().Count -eq 0) {
            Add-ItemLanguage -Item $item -TargetLanguage $lang -IfExist Skip
        }
            
        if ($null -ne $field) {
            $targetLangItem = Get-Item -Path $item.Paths.FullPath -Language $lang -ErrorAction SilentlyContinue;
            $targetLangItem.Editing.BeginEdit()
            $targetLangItem[$field] = $value
            $targetLangItem.Editing.EndEdit() | Out-Null
        }
    }
    
    function Transfer-Update($website){
        $dataSourcePath ="/sitecore/content/EasyJet/$website/Home/Booking/Page Components/Transfer Section"
        
        $dataSourceItem = Get-Item -Path $dataSourcePath
        
        if($dataSourceItem -ne $null) {
            $dataSourceItem.Editing.BeginEdit()
            $dataSourceItem["PrivateFeesDescription"] = "Our transfer partner may charge additional fees for taking sport equipment. You will need to contact the customer support centre after booking."
            $dataSourceItem.Editing.EndEdit() | Out-Null
            
            Write-Host "Transfer Section Datasource updated for $website"
        } else {
            Write-Host "Datasource item not found for $website"
            return
        }
        
        # Add translations on Holidays
        if($website -eq "Holidays"){
            foreach ($lang in $translations.Keys) {
                create-lang-version -item $dataSourceItem -lang $lang -field "PrivateFeesDescription" -value $translations[$lang]["PrivateFeesDescription"]
            }
            Write-Host "Translations added for $website"
        }
    }
    
    # Update for specified websites
    Transfer-Update -website "Holidays"
    Transfer-Update -website "TradePortal"
    
- ID: "ba3f86a2-4a1c-4d78-b63d-91c2779c1b5e"
  Hint: __Sortorder
  Value: 1030
Languages:
- Language: en
  Versions:
  - Version: 1
    Fields:
    - ID: "25bed78c-4957-4165-998a-ca1b52f67497"
      Hint: __Created
      Value: 20240709T095015Z
