export AWS_PROFILE := env('AWS_PROFILE', 'ejh-web-dev')

help:
  @just --list

init *args:
  terraform init -backend-config=".config.local.tfbackend" {{ args }}

workspace:
  terraform workspace select 'Global-Dev-Lambda-CloudinaryContentSync'

plan:
  terraform plan --var-file='vars/variables.local.dev.tfvars'

apply:
  terraform apply --var-file='vars/variables.local.dev.tfvars'

fmt:
  terraform fmt -recursive
