/**
 * For the cases when need return offers from everywhere
 */
export const GEOGRAPHY_ALL_CODE = 'ALL';

/**
 * For the cases when need return offers will all available departures
 */
export const DEPARTURE_ALL_CODE = 'ALL';
