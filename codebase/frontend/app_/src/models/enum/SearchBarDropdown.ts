export enum SearchBarDropdown {
    From = 'FROM',
    To = 'TO',
    When = 'WHEN',
    Who = 'WHO',
    Recent = 'RECENT',
}

export enum SearchBarDropdownFooterButton {
    Clear = 'Clear',
    Close = 'Close',
    Apply = 'Apply',
}
